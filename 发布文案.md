# GitHub 仓库 / Releases 描述文案

> 以下文案可直接复制到 GitHub 使用。仓库名建议用 `OCharSilo`。

---

## 一、仓库 About（仓库简介，~350 字以内）

```
OCharSilo — 离线优先的 OC稿件管理器。纯前端单文件应用，数据只存在你自己的浏览器里。
```

---

## 二、Releases 发布说明（建议首个 Release 命名为 `v1.0.0`）

### OCharSilo v1.0.0（首次公开版）

**OCharSilo** 是一个离线优先的 OC（原创角色）管理工具，帮你整理储存稿件。纯前端 **单文件应用**，双击 `index.html` 即可使用，无需服务器、无需联网。

#### ✨ 功能

- **稿件管理**：标题、作者、角色、标签、价格、附件、画廊封面
- **自定义字段**：文本 / 下拉 / 滑块 / 进度等，可保存模板
- **主题系统**：内置 Y2K、新拟物风、童话梦幻系、复古未来主义、昭和宣纸、杂志编辑等，支持导入导出 `.oc-theme`
- **离线优先**：基于 IndexedDB 本地存储，断网也能用
- **云同步（可选）**：可把自己配置的 WebDAV / S3（R2、MinIO、阿里云 OSS、腾讯云 COS）作为同步后端
- **可安装为 PWA**：装到手机主屏 / 桌面，全屏运行

#### 🚀 快速开始

直接用浏览器打开仓库里的 `index.html` 即可使用。

#### 🌐 部署为可安装的 PWA（可选）

```bash
node build_pwa.js
```

生成 `dist/` 静态站点，上传到任意 https 静态托管（GitHub Pages / Cloudflare Pages / Vercel / Netlify）即可让别人「安装到主屏」。

#### 📜 许可证（重要）

本项目以 **非商业许可证** 发布（见 `LICENSE`）：

- ✅ 允许个人 / 非商业用途免费使用、修改、再分发；
- ❌ **禁止销售软件副本**；
- ❌ **禁止以本软件直接营利**。

如果你希望用于商业场景，请先联系作者授权。

---

## 三、Releases 资产（Asset）建议上传

- `index.html`（应用本体，单文件）
- `dist.zip`（可选：把 `node build_pwa.js` 生成的 `dist/` 打包上传，方便别人下载安装版）
